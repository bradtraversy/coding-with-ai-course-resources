# Item CRUD Architecture

Now we are going to start working on item CRUD. We have a document that describes what the items from each type looks like. We can use that to create a CRUD architecture document, which can then be used to implement CRUD piece by piece.

Let's create a new research document at `/context/research/item-crud-research.md`. Find the content in the course file and it it to your own.

Now let's run:

```text
/research item-crud-research.md
```

This will generate a new file at `docs/item-crud-architecture.md`.

Study that file so that you understand how CRUD will be implemented.

Now we have our entire CRUD plan outlined.

Commit and push the changes.

In the next lesson, we will start the listings page feature.
