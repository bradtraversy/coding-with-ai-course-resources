# Pinned Items

We have a field in the items table called `isPinned`. I want to make it so we can pin then with the icon and then those will get put to the beginning of the listings page for that item.

## Feature Spec

Let's create a spec file for this one at `context/features/pinned-spec.md`

Now run `/feature start`.

Check pinning functionality then run:

```text
/feature test
/feature review
/feature complete
clear
```
