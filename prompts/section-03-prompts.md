# Section 03 Prompts

## Python Example Prompts

```text
Create me a python script that will fetch a Chuck Norris joke from the api.chucknorris.io api
```

```text
Create the file but call it joke.py
```

```claude
Run the file
```

```text
Update joke.py to ask the user if they want to hear another joke. If they type yes, fetch another one. Keep going until they type no.
```

## Boilerplate Prompts

After we generate a Next.js app, we ran these to do a bit of cleanup:

```text
Clean up the boilerplate. Replace page.tsx with a simple component that just shows an h1 that says "Devstash". Remove all the default styles from globals.css except the Tailwind import.
```

```text
 Lets get rid of the svg files in the public folder
```

```text
Look at @package.json
```
